class Player {
    constructor(initialCards, bet, cards) {
        this.initialCards = initialCards; //this will be the array of cards
        this.bet = bet; //this will be the bet that the dealer can collect
        this.cards = cards;
    }

    receiveCards() {
        this.initialCards += cards;
    }

    
}